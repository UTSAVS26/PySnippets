
def format_to_2_decimal(num):
    """
    Format a number to two decimal places.

    Args:
        num (float or int): The number to format.

    Returns:
        str: The number formatted to two decimal places as a string.
    """
    return f"{num:.2f}"
